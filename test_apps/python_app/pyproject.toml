[tool.poetry]
name = "example"
version = "1.2.3"
description = ""
authors = ["Stephen McGinty <stephen.f.mcginty@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.6"
origen = { path = "../../python" }
python_plugin = { path = "../python_plugin" }

[tool.poetry.dev-dependencies]
pytest = "^3.0"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"

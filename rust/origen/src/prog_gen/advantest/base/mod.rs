use super::super::TestProgram;
use crate::Result;

// This is called at the start of a test program generation run to define
// all of the built-in Advantest test method templates
pub fn define_test_lib(prog: &mut TestProgram) -> Result<()> {
    Ok(())
}

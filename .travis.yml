matrix:
  include:
    - language: rust
      rust:
        -  1.41.0-nightly
      script:
        - cd rust/origen/pyapi
        - cargo build
        - cd ../
        - cargo build --verbose --all --workspace --bins
        - cargo test
    # - language: python
    #   python: 3.8
    #   before_script:
    #     - cd example
    #       origen setup
    #   script:
    #     - poetry run pytest